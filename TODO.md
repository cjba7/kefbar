<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
# TODO

Known follow-ups, roughly in priority order.

## Distribution
- [ ] **Notarise the release.** Sign with a personal Developer ID (not the ORDR org),
      notarise the DMG, and staple the ticket, so users no longer need to right-click
      Open on first launch. Local builds stay self-signed for now.

## Polish
- [ ] **App icon.** There is no custom icon yet, so the app uses the system default. An
      icon would also let the DMG show a branded window and volume icon.
- [ ] **README screenshots.** Add `docs/screenshots/panel.png` and `settings.png`.

## Maybe
- [ ] **Cmd-, for Settings.** The app has no visible menu, so Settings opens only from
      the menu-bar button. Add a keyboard shortcut if it turns out to be missed.
