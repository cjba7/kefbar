<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
# Screenshots

The main README references two images here — drop them in with these exact names:

- **`panel.png`** — the menu-bar panel. Click the speaker icon in the menu bar to
  open it; capture just the panel. Light mode, Olive (Dark Olive) palette looks best
  as the hero.
- **`settings.png`** — the Settings window. The **UI** tab shows off the palettes and
  appearance options nicely; the **Speakers** tab is also a good choice.

PNG; retina/2× is fine — the README sizes them down.
